% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getters.R
\name{getMetaComm}
\alias{getMetaComm}
\alias{getLocalComm}
\alias{getPhylo}
\alias{getState}
\alias{getMetaComm,roleModel-method}
\alias{getLocalComm,roleModel-method}
\alias{getPhylo,roleModel-method}
\alias{getState,roleModel-method}
\title{Getter functions for `roleModel` objects}
\usage{
getMetaComm(x)

getLocalComm(x, snapshot = Inf)

getPhylo(x, snapshot = Inf)

getState(x, snapshot = Inf)

\S4method{getMetaComm}{roleModel}(x)

\S4method{getLocalComm}{roleModel}(x, snapshot = Inf)

\S4method{getPhylo}{roleModel}(x, snapshot = Inf)

\S4method{getState}{roleModel}(x, snapshot = Inf)
}
\arguments{
\item{x}{A `roleModel` object.}

\item{snapshot}{Integer index of the desired snapshot. Use `Inf` (default)
for the last saved snapshot.}
}
\description{
Extract components from a `roleModel` at a given snapshot.
}
\details{
`getMetaComm` takes only the `roleModel` argument because the
  metacommunity does not vary across snapshots; it returns the `metaComm`
  from the initial state. `getLocalComm`, `getPhylo`, and `getState` accept
  a `snapshot` argument. `getState` with `snapshot = Inf` is equivalent to
  the existing `getFinalState` for a `roleModel`.
}
